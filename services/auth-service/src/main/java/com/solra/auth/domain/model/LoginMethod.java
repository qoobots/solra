package com.solra.auth.domain.model;

/**
 * Login method enum.
 */
public enum LoginMethod {
    PASSWORD,
    PHONE_CODE,
    EMAIL_CODE,
    OAUTH_WECHAT,
    OAUTH_APPLE,
    OAUTH_GOOGLE,
    BIOMETRIC
}
