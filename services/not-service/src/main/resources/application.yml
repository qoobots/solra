spring:
  application:
    name: not-service
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}

server:
  port: ${SERVER_PORT:8093}

management:
  server:
    port: ${MANAGEMENT_PORT:9093}
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus
      base-path: /actuator
  endpoint:
    health:
      show-details: always
      probes:
        enabled: true
      group:
        readiness:
          include: readinessState,db,redis,apns,fcm
        liveness:
          include: livenessState
  metrics:
    export:
      prometheus:
        enabled: true
    tags:
      application: not-service

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
