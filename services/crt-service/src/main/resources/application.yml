spring:
  application:
    name: crt-service
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}

server:
  port: ${SERVER_PORT:8094}

management:
  server:
    port: ${MANAGEMENT_PORT:9094}
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus
      base-path: /actuator
  endpoint:
    health:
      show-details: always
      probes:
        enabled: true
      group:
        readiness:
          include: readinessState,db,redis,cdn,asset-storage
        liveness:
          include: livenessState
  metrics:
    export:
      prometheus:
        enabled: true
    tags:
      application: crt-service

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
